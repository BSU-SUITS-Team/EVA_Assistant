# Core dependencies - telemetry fetching
requests>=2.28,<3.0

# Optional: LLM-based natural language formatting
# These packages are optional; the assistant falls back to deterministic formatting if unavailable
# To enable LLM features, ensure Ollama is running: ollama serve
langchain-core>=0.1.0,<0.2
langchain-ollama>=0.0.1
